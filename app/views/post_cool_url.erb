You posted // <%= @user_input %>  // into the 'POST' request form. Pay attention to how you were redirected to /cool_url. But the information you posted didn't make it into the URL and was kept "secret".But if you inspect NETWORK tab in dev tools, you'll see that the information you entered was sent to the server and "returned back" to the browser.

Also, you selected the // <%= @dropdown %>  // option and sent it via the 'post' method.

And lastly, testing the // <%= @post_price%> price I entered